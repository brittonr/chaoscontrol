## Decision

Legacy accepted legacy specs stay available as archived migration input. Native Cairn validation owns the active and accepted specification set.

## Rationale

A direct copy of the legacy accepted specs into `.cairn/specs/` fails current Cairn substance checks. Archiving preserves the historical text without weakening native validation.
