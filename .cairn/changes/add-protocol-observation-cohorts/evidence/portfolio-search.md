# Protocol cohort validation

## Success contract

The change must admit bounded profiles, transport opaque records, assemble exact cohorts, and retain replay-bound consumer results.
Fresh tests must cover every requirement before lifecycle mutation.
Compilation, task counts, runtime pass fields, and compact coverage slots are not completion evidence.

The source boundary is this change worktree at base `31300fa1a2d29c7496e8316f065c156f80343143`.
Mantle and dirty primary worktrees remain outside this work.
The published scheduler and branch-marker contracts supply identity and snapshot mechanisms.
No sibling supplies protocol semantics or oracle authority.

## Search budget

This pass covers one change and three review lenses.
Each lens uses source inspection, deterministic fixtures, and one corrective validation round before reassessment.
Retrieval stays local unless an exact provider contract is missing.
No subagents participate. These serial passes are correlated.
Allowed outcomes are validated, blocked, exhausted, or a required user decision.

## Approach registry

| Family | Mechanism | Evidence | State | Next check |
| --- | --- | --- | --- | --- |
| Admission | Exact profile, origin, schema, and BLAKE3 validation before retention | 23 focused integration cases pass inside the all-target, all-feature package suites. Seven invalid Nickel profiles fail. | validated | Final pinned Nix and Octet results |
| Accounting | Bounded source journals with exact duplicates and final-drain checks | Changed duplicates, post-final records, mixed executions, missing suffixes, and fault-run resets fail closed. | validated | Final pinned Nix result |
| Evidence | Revalidate cohort, oracle, snapshot, and receipt linkage before acceptance | Forged complete flags, changed oracle results, and incorrect marker bindings fail. Marker status reports only identity linkage. | audit | Exercise `Session::replay` with a stored parent and an executed guest continuation |

## Current audit risks

Public data fields require revalidation at every evidence boundary.
The retained source journal and derived-field reconstruction now enforce that boundary.
Host rejection counts remain separate from guest loss counters and survive fault-run resets.
A matching digest still cannot prove process authentication or snapshot restorability.
Unsupported projection support cannot hide invalid records.
Consumer oracle invocation checks bounded input, adapter identity, and exact result-to-cohort binding.

The later audit reproduced two defects before correction.
`attempts/reset-status-before.log` shows loss erasure after a fault-run reset and an unsupported `reachable` marker status.
The corrected positive and negative fixtures pass in `verification-2026-09-05/all-target-tests.log`.

This pass used two corrective audits over the same three serial lenses.
The results are correlated, not independent agent judgments.
Repository gates and a real protocol-aware session replay remain completion requirements.
The scoped Nix check passes all 23 integration cases and seven invalid profiles.
The KVM case passes host dispatch and snapshot retention only.

The terminal state is blocked for lifecycle completion.
Octet rejects the inherited guest-probe defaults, and its output includes unresolved new-code warnings.
The product-scope guard rejects the missing intent for the unrelated `adopt-campaign-core` package.
The evidence lens also retains the unverified `Session::replay` effect path.

The smallest next checks are a stored-parent guest continuation, a clean Octet review, and an owner-reviewed Campaign intent.
No lifecycle mutation or publication is authorized by the focused test count alone.
