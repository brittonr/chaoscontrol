# VM Cohort package repair

## Scope

VM Cohort owns the generated standard profile. Its immutable repair is `0953ab17d2f4e318567a57925dc8fe30669d5b68` at `rad://z2QJLUqyAZnnHPiZQ1BFjLsX9ush3`.

The repair keeps the Nickel source and historical workspace artifact. It adds identical generated bytes inside the conformance crate. The embed no longer requires a parent workspace directory. A fresh export must match both artifacts. A missing crate-local profile must fail compilation.

The owner published the repair on `cairn/package-conformance-profile-20260907` under publisher `z6MksnXbFoE8zkCkGWhHc8zuxpnEUhrJHv2KECRV4GSv9gkx`. Private visibility remains unchanged. Publication and acquisition here refer to signed local Radicle storage, not a new remote replica or public access grant.

## Acquisition and checks

The root Radicle URL accepts an exact-object fetch. Stock Cargo only fetched the default branch and could not resolve the candidate. The existing scoped transport now admits this exact URL alongside Campaign. No cache preload, sibling path, namespace HEAD change, or fallback exists.

Nix regenerated `flake.lock`. Cargo regenerated `Cargo.lock`. Cargo also corrected nine Windows dependency edges to existing locked versions. No registry package version changed.

- Owner baseline passed before implementation.
- All eight owner Nix checks passed, including the full pinned Octet gate, positive relocation, and negative missing-profile controls.
- The owner repeated those checks from an archive of the immutable revision.
- A new Cargo home acquired the repair with no Git cache. Only the registry cache was shared. Adapter tests passed: nine tests and three existing ignored KVM tests.
- The fresh Cargo database passed `git fsck --strict`. Its revision archive matched the owner archive byte for byte.
- `checks.x86_64-linux.vm-cohort-packaged-build` compiled the actual Nix-vendored conformance crate and passed the same nine adapter tests. It uses no parent configuration copy or prebuilt workspace artifacts.

## Limits

The full ChaosControl Nix check advanced to two existing Octet errors in `crates/chaoscontrol-protocol/src/branch_marker.rs`. It did not pass. These errors concern implicit defaults on optional fields.

A later filesystem quota failure interrupted the broader local test retry. Cleanup removed unused Cargo build output, not source or evidence. Further test results require separate logs.

This repair proves package acquisition and conformance packaging. It does not complete the Campaign runtime adapter, durable journal, KVM adoption, fleet rollout, or lifecycle closure.

## Retained logs

Logs remain under `campaign/.pi/complete-20260906/`:

- `vm-profile-frozen.log`
- `vm-profile-fresh-consumer.log`
- `vm-profile-fetched.tar` and `vm-profile-reference.tar`
- `vm-profile-consumer-full-nix.log`
- `vm-profile-packaged-consumer-resume.log`

VM Cohort maintains the profile contract. ChaosControl maintains the vendored consumer check and exact dependency pins.
