[package]
name = "my-service-chaos-workload"
version = "0.1.0"
edition = "2021"
publish = false

# Keep this copyable template out of ChaosControl's own workspace when checked in.
[workspace]

[dependencies]
chaoscontrol-sdk = { path = "../../../crates/chaoscontrol-sdk" }
linkme = "0.3"
serde_json = "1"
