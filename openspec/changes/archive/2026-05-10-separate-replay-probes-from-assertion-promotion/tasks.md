# Tasks

- [x] Define replay-probe vs instrumentation promotion semantics.
- [x] Render replay-probe failures separately from ordinary non-passing assertion gaps.
- [x] Make the static checker fail closed if replay-probe signal counts are hidden or drift.
- [x] Regenerate assertion-readiness status and verify focused gates.
